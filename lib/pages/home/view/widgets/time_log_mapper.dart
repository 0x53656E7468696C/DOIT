import 'package:flutter/material.dart';

class TimeLogMapper extends StatelessWidget {
  const TimeLogMapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}