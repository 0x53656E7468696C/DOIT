import 'package:flutter/material.dart';

class DOITPomoClock extends StatelessWidget {
  const DOITPomoClock({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 240,
      width: 240,
      child: Container(),    );
  }
}