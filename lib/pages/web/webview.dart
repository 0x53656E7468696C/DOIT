import 'package:flutter/material.dart';

class DOITWeb extends StatelessWidget {
  const DOITWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}